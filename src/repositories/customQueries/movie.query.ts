// const query = `select distinct mv.* from theatre as th
// inner join screen as sc
// on sc."theatreId" = th.id
// inner join show as sh on
// sh."screenId" = sc.id
// inner join movie as mv on
// mv.id = sh."movieId" where th."cityId" = '${cityId}'`;
